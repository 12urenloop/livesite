version: '3'

services:
    live_site:
        build:
            context: .
        container_name: live_site
        restart: unless-stopped
        env_file: .env
        ports:
            - "4000:80"
